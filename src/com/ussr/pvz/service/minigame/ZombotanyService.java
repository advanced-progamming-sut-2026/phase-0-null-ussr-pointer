package com.ussr.pvz.service.minigame;

public class ZombotanyService {
    // TODO: Zombotany Mechanics
    // create the minigame interactions here (e.g., exposing player actions)
    // to mirror the structure of IZombieBehavior/IZombieService.
    //
    // TODO(zombotany-zombie-types): none of the 4 special zombie types exist yet in
    //  ZombieFactory/zombies.json. Must be created first (see TODO in ZombieFactory.java):
    //    - peashooter-zombie: shoots a straight-line pea leftward at plants, otherwise a normal zombie
    //    - wall-nut-zombie: very high HP, normal zombie speed
    //    - jalapeno-zombie: self-destructs 10s after spawn, burns/destroys every plant in its row
    //    - squash-zombie: fast movement, on hitting a plant destroys itself AND that plant instantly
    //
    // TODO(zombotany-level-flow): once the zombie types exist, this service (or ZombotanyBehavior,
    //  which is already registered in LevelFactory) should just reuse the normal level flow
    //  (plant selection -> waves -> win/lose) — per spec nothing else differs from a regular level.

}